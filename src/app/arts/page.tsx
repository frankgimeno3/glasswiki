import React, { FC } from 'react';

interface ArtsProps {
  
}

const Arts: FC<ArtsProps> = ({ }) => {
  return (
    <div>Listado de artículos en orden cronológico</div>
  );
};

export default Arts;